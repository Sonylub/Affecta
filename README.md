# Affecta - Биполярный трекер

**Affecta** — веб-приложение для отслеживания состояния и настроения при биполярном расстройстве. Проект разработан для защиты дипломной работы студента 20 лет.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Основной функционал](#основной-функционал)
- [Технологический стек](#технологический-стек)
- [Установка и настройка](#установка-и-настройка)
- [Структура проекта](#структура-проекта)
- [База данных](#база-данных)
- [API и маршруты](#api-и-маршруты)
- [Алгоритм определения фаз](#алгоритм-определения-фаз)
- [Использование](#использование)
- [Разработка](#разработка)

## 🎯 Описание проекта

Affecta — это специализированное веб-приложение для ежедневного отслеживания симптомов биполярного расстройства. Система позволяет пользователям:

- Ведение ежедневных записей о состоянии
- Отслеживание приёма лекарств
- Анализ динамики состояния через графики и статистику
- Автоматическое определение фаз биполярного расстройства
- Генерация PDF отчётов для врачей

## ✨ Основной функционал

### 1. Аутентификация и управление пользователями

- **Регистрация** новых пользователей с валидацией данных
- **Авторизация** через Flask-Login с поддержкой сессий
- **Безопасное хранение паролей** через bcrypt
- Автоматический редирект на страницу записи после входа

### 2. Ежедневные записи (Entry)

#### Основные состояния
- **Депрессивное состояние**: нет / лёгкое / умеренное / тяжёлое
- **Маниакальное состояние (гипомания)**: нет / лёгкое / умеренное / тяжёлое
- **Раздражительность**: нет / лёгкая / умеренная / сильная
- **Тревога**: нет / лёгкая / умеренная / сильная
- **Психотические симптомы**: да / нет
- **Психотерапия сегодня**: да / нет

#### Пользовательские состояния
Пользователь может создавать собственные состояния с различными типами отметок:
- **Бинарный** (да/нет)
- **Категориальный** (нет / лёгкое / умеренное / тяжёлое)
- **Числовая шкала** (0-10)
- **Множественный выбор** (чекбоксы с пользовательскими вариантами)

#### Сон
- **Часы сна**: ползунок от 0 до 24 часов (шаг 0.5)
- **Качество сна**: плохое / среднее / хорошее

#### Лекарства
- Динамический список лекарств пользователя
- Для каждого лекарства указывается:
  - Название препарата
  - Дозировка (мг)
  - Время приёма (утро / день / вечер / ночь)
  - Частота приёма (ежедневно / по необходимости)
- Отметка о приёме лекарства в конкретный день (да/нет)

#### Навигация по датам
- Стрелочная навигация (предыдущий/следующий день)
- Календарная кнопка для быстрого выбора даты
- Человекочитаемое отображение даты (например, "15 января")
- Блокировка навигации на будущие даты

#### Автоматическое определение типа дня
Система автоматически определяет фазу биполярного расстройства на основе:
- Текущей записи
- Статистики за последние 7 дней
- Алгоритма с учётом устойчивости симптомов и быстрых переключений

**Типы дней:**
- **Нормальный** — отсутствие выраженных симптомов
- **Депрессивный** — наличие депрессивных симптомов
- **Гипоманиакальный** — наличие маниакальных симптомов
- **Смешанный** — одновременное наличие депрессивных и маниакальных симптомов или быстрые переключения

#### Заметки
- Текстовое поле для свободных заметок о дне

### 3. Дашборд (Dashboard)

#### Текущая фаза
- Крупное отображение текущей фазы с цветовой индикацией
- Кнопка с объяснением, почему определена эта фаза
- Дата последней записи

#### История фаз
- Интерактивный график истории фаз за выбранный период
- Цветовая кодировка фаз
- Поддержка различных диапазонов: 7 дней, месяц, 6 месяцев, год, произвольный

#### Статистика сна
- График продолжительности сна
- Статистика: среднее, минимум, максимум часов сна
- График качества сна
- Сворачиваемые/разворачиваемые секции

#### Детальная аналитика
- Графики состояний (настроение, энергия, раздражительность, тревога)
- Интерактивные графики с Chart.js
- Сворачиваемые секции для экономии места

#### Последние записи
- Список последних 7 записей с краткой информацией
- Отображение типа дня и заметок

### 4. Аналитика (Analytics)

#### Статистические карточки
- Всего записей за период
- Среднее настроение с трендом
- Средняя энергия с трендом
- Средний сон с трендом

#### Графики
- **Динамика настроения**: линейный график за период
- **Динамика энергии**: линейный график за период
- **Продолжительность сна**: столбчатая диаграмма
- **Распределение типов дней**: круговая диаграмма

#### Кастомные трекеры
- Автоматическое отображение графиков для пользовательских трекеров
- Поддержка различных типов трекеров (слайдер, чекбокс, число)

#### Таблица данных
- Полная таблица всех записей за период
- Сортировка по дате
- Клик по строке открывает детальное модальное окно

#### Периоды анализа
- Последние 7 дней
- Последние 30 дней
- Последние 90 дней

### 5. Генерация PDF отчётов

#### Настройка отчёта
- Выбор диапазона дат
- Выбор разделов для включения в отчёт:
  - История фаз
  - Статистика сна
  - График состояний
  - Заметки
- Комментарий к отчёту

#### Содержимое отчёта
- **Шапка**: название, период, пользователь, дата формирования
- **История фаз**: график с цветовой кодировкой
- **Статистика сна**: среднее, минимум, максимум, график
- **График состояний**: настроение, энергия, раздражительность, тревога
- **Заметки**: все заметки за период с датами

#### Технические особенности
- Поддержка кириллицы через системные шрифты Windows
- Автоматическое форматирование дат
- Профессиональное оформление с использованием reportlab и matplotlib

### 6. Управление данными

#### Лекарства
- Добавление новых лекарств с полной информацией
- Редактирование существующих лекарств
- Удаление лекарств (с удалением всех отметок приёма)

#### Пользовательские состояния
- Создание состояний с выбором типа отметки
- Редактирование состояний (включая изменение типа)
- Удаление состояний (с удалением всех значений в записях)
- Для типа "множественный выбор": управление вариантами чекбоксов

#### Кастомные трекеры
- Создание трекеров с различными типами (слайдер, чекбокс, число)
- Настройка диапазонов значений
- Автоматическое отображение в аналитике

## 🛠 Технологический стек

### Backend
- **Flask 2.3.3** — веб-фреймворк
- **Flask-Login 0.6.3** — управление сессиями и аутентификация
- **PyMySQL 1.1.0** — подключение к MySQL
- **bcrypt 4.0.1** — хеширование паролей
- **python-dotenv 1.0.0** — управление переменными окружения

### Frontend
- **HTML5 + CSS3** — разметка и стили
- **Tailwind CSS** — утилитарный CSS-фреймворк (через CDN)
- **JavaScript (ES6+)** — интерактивность
- **Chart.js** — построение графиков
- **Vanilla JS** — без дополнительных фреймворков

### База данных
- **MySQL** — реляционная база данных
- **UTF-8 (utf8mb4)** — поддержка кириллицы

### Генерация отчётов
- **reportlab 4.0.7** — генерация PDF
- **matplotlib 3.7.2** — построение графиков для PDF
- **Pillow 10.1.0** — обработка изображений

### Дополнительные библиотеки
- **python-dateutil 2.8.2** — работа с датами
- **gunicorn 21.2.0** — WSGI-сервер для продакшена

## 📦 Установка и настройка

### Требования
- Python 3.8+
- MySQL 5.7+ или 8.0+
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонирование репозитория** (если используется Git)
```bash
git clone <repository-url>
cd Affecta
```

2. **Создание виртуального окружения**
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

3. **Установка зависимостей**
```bash
pip install -r requirements.txt
```

4. **Настройка базы данных**
   - Создайте базу данных MySQL
   - Выполните SQL-скрипт из `database/init_db.sql`:
   ```bash
   mysql -u root -p < database/init_db.sql
   ```

5. **Настройка переменных окружения**
   Создайте файл `.env` в корне проекта:
   ```env
   SECRET_KEY=your-secret-key-here
   DB_HOST=localhost
   DB_USER=root
   DB_PASS=your-password
   DB_NAME=Treker
   DB_PORT=3306
   ```

6. **Запуск приложения**
```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 📁 Структура проекта

```
Affecta/
├── app.py                 # Главный файл приложения Flask
├── models.py              # Модели базы данных и класс Database
├── routes.py              # Все маршруты и API endpoints
├── report_generator.py    # Генератор PDF отчётов
├── requirements.txt       # Зависимости Python
├── README.md              # Документация
│
├── database/
│   └── init_db.sql        # SQL-скрипт инициализации БД
│
├── templates/
│   ├── base.html          # Базовый шаблон
│   ├── index.html         # Главная страница
│   ├── login.html         # Страница входа
│   ├── register.html      # Страница регистрации
│   ├── entry.html         # Страница дневной записи
│   ├── dashboard.html     # Дашборд
│   ├── analytics.html     # Страница аналитики
│   ├── report_settings.html  # Настройки отчёта
│   └── pdf_template.html  # Шаблон для PDF (не используется напрямую)
│
└── static/
    ├── css/
    │   └── style.css      # Дополнительные стили
    ├── js/
    │   ├── main.js        # Общие утилиты
    │   ├── entry.js       # Логика страницы записи
    │   ├── dashboard.js   # Логика дашборда
    │   └── analytics.js   # Логика аналитики
    └── images/
        ├── affecta-logo.svg
        ├── depressive-phase.png
        ├── hypomanic-phase.png
        └── mixed-phase.png
```

## 🗄 База данных

### Основные таблицы

#### `users`
- Хранение пользователей системы
- Поля: `id`, `username`, `password_hash`, `created_at`

#### `entries`
- Ежедневные записи состояния
- Поля:
  - Основные: `user_id`, `entry_date`, `notes`
  - Состояния: `depressive_state`, `manic_state`, `irritable_state`, `anxious_state`
  - Бинарные: `psychotic_symptoms`, `psychotherapy`
  - Сон: `sleep_hours`, `sleep_quality`
  - Тип дня: `day_type` (depressive/normal/hypomanic/mixed)
  - Устаревшие числовые поля: `mood`, `irritability`, `anxiety`, `energy` (для обратной совместимости)

#### `medications`
- Список лекарств пользователя
- Поля: `id`, `user_id`, `name`, `dosage_mg`, `time_of_day`, `frequency`

#### `med_intakes`
- Отметки о приёме лекарств для каждой записи
- Поля: `entry_id`, `med_id`, `taken` (full/half/none)

#### `custom_trackers`
- Пользовательские трекеры
- Поля: `id`, `user_id`, `name`, `type`, `min_value`, `max_value`

#### `custom_values`
- Значения кастомных трекеров для записей
- Поля: `entry_id`, `tracker_id`, `value`

#### `custom_states`
- Пользовательские состояния
- Поля: `id`, `user_id`, `name`, `mark_type` (binary/categorical/numeric/multi_checkbox)

#### `custom_state_values`
- Значения пользовательских состояний для записей
- Поля: `entry_id`, `state_id`, `value`

#### `custom_state_options`
- Варианты для состояний типа multi_checkbox
- Поля: `id`, `state_id`, `label`, `position`

### Индексы
- Оптимизированные индексы для быстрого поиска по `user_id` и `entry_date`
- Уникальные ключи для предотвращения дубликатов

## 🔌 API и маршруты

### Страницы (GET)

- `/` — главная страница (редирект на entry для авторизованных)
- `/register` — регистрация
- `/login` — авторизация
- `/logout` — выход
- `/entry` — страница дневной записи
- `/dashboard` — дашборд
- `/analytics` — аналитика
- `/report_settings` — настройки отчёта

### API Endpoints

#### Записи
- `POST /save_entry` — сохранение/обновление записи
- `GET /get_entry/<date>` — получение записи за дату

#### Лекарства
- `POST /add_medication` — добавление лекарства
- `POST /update_medication` — обновление лекарства
- `POST /delete_medication` — удаление лекарства

#### Пользовательские состояния
- `POST /add_custom_state` — добавление состояния
- `POST /update_custom_state` — обновление состояния
- `POST /delete_custom_state` — удаление состояния

#### Данные для графиков
- `GET /get_dashboard_data` — данные для дашборда (параметры: `start_date`, `end_date`)
- `GET /get_analytics_data` — данные для аналитики (параметр: `period`)

#### Отчёты
- `GET /generate_report` — генерация PDF отчёта (параметры: `start_date`, `end_date`, `sections[]`, `comment`)

## 🧮 Алгоритм определения фаз

Система использует статистический алгоритм для определения фазы биполярного расстройства:

### Входные данные
- Текущая запись (состояния, сон)
- Записи за последние 7 дней

### Шаги алгоритма

1. **Оценка состояний**
   - Каждому состоянию присваивается вес:
     - `none`: 0
     - `mild`: 3
     - `moderate`: 6
     - `severe`: 9

2. **Учёт сна**
   - Сон < 3 часов усиливает маниакальные симптомы
   - Сон > 12 часов усиливает депрессивные симптомы

3. **Определение эпизода для каждого дня**
   - Эпизод считается, если:
     - Оценка состояния >= 6, ИЛИ
     - Оценка >= 3 И (сон <= 3 ИЛИ сон >= 12)

4. **Анализ быстрых переключений**
   - Если за период >= 2 переключения между депрессией и гипоманией → **Смешанная фаза**

5. **Анализ одновременных симптомов**
   - Если в текущей записи одновременно депрессивные И маниакальные симптомы → **Смешанная фаза**

6. **Статистический анализ**
   - Если фаза устойчива >= 50% дней за период → определяется эта фаза
   - Иначе определяется фаза на основе текущей записи

7. **Результат**
   - Возвращается тип дня и объяснение определения

### Примеры объяснений
- "Смешанная фаза определена на основе анализа паттернов за последние 7 дней"
- "Обнаружены быстрые переключения между депрессией и гипоманией: 3 переключения за период"
- "Депрессивная фаза определена на основе статистики за последние 7 дней"
- "Фаза устойчива: 5 из 7 дней (71%)"

## 💻 Использование

### Регистрация и вход
1. Перейдите на главную страницу
2. Нажмите "Регистрация"
3. Введите имя пользователя и пароль (минимум 6 символов)
4. После регистрации автоматически выполнится вход

### Создание ежедневной записи
1. На странице "Дневная запись" выберите дату
2. Заполните состояния (депрессивное, маниакальное, раздражительность, тревога)
3. Отметьте бинарные параметры (психотические симптомы, психотерапия)
4. Укажите сон (часы и качество)
5. Отметьте приём лекарств (если добавлены)
6. Заполните пользовательские состояния (если созданы)
7. Добавьте заметки (опционально)
8. Нажмите "Сохранить"

### Добавление лекарства
1. На странице записи нажмите "+ Добавить лекарство"
2. Заполните форму:
   - Название препарата (обязательно)
   - Дозировка в мг (опционально)
   - Время приёма (опционально)
   - Частота приёма
3. Нажмите "Добавить"

### Создание пользовательского состояния
1. На странице записи нажмите "+ Добавить состояние"
2. Введите название состояния
3. Выберите тип отметки:
   - **Да/Нет** — бинарный выбор
   - **Категориальный** — нет/лёгкое/умеренное/тяжёлое
   - **Числовая шкала** — ползунок 0-10
   - **Множественный выбор** — чекбоксы с пользовательскими вариантами
4. Для множественного выбора добавьте варианты
5. Нажмите "Добавить"

### Просмотр дашборда
1. Перейдите на страницу "Дашборд"
2. Выберите период (7 дней, месяц, 6 месяцев, год, произвольный)
3. Просмотрите:
   - Текущую фазу с объяснением
   - График истории фаз
   - Статистику сна
   - Детальные графики состояний
   - Последние записи

### Аналитика
1. Перейдите на страницу "Аналитика"
2. Выберите период (7/30/90 дней)
3. Просмотрите:
   - Статистические карточки
   - Графики динамики
   - Распределение типов дней
   - Таблицу всех записей
4. Клик по строке таблицы открывает детальную информацию

### Генерация PDF отчёта
1. Перейдите на страницу "Экспорт отчёта" (из дашборда) или `/report_settings`
2. Выберите диапазон дат
3. Выберите разделы для включения в отчёт
4. Добавьте комментарий (опционально)
5. Нажмите "Сгенерировать отчёт"
6. PDF файл автоматически скачается

## 🔧 Разработка

### Структура кода

#### `app.py`
- Инициализация Flask приложения
- Настройка Flask-Login
- Регистрация Blueprint

#### `models.py`
- Класс `Database` — все методы работы с БД
- Класс `User` — модель пользователя для Flask-Login
- Методы CRUD для всех сущностей

#### `routes.py`
- Все маршруты приложения
- Функция `determine_phase_with_statistics()` — алгоритм определения фаз
- API endpoints для фронтенда

#### `report_generator.py`
- Класс `ReportGenerator` — генерация PDF
- Методы построения графиков через matplotlib
- Поддержка кириллицы

### JavaScript модули

#### `entry.js`
- Управление формой записи
- Навигация по датам
- Обработка состояний и лекарств
- Сохранение через AJAX

#### `dashboard.js`
- Инициализация графиков Chart.js
- Загрузка данных через API
- Управление диапазонами дат
- Отображение текущей фазы

#### `analytics.js`
- Построение аналитических графиков
- Расчёт статистики и трендов
- Управление таблицей данных

### Расширение функционала

#### Добавление нового типа состояния
1. Добавьте новый тип в `custom_states.mark_type` (ENUM в БД)
2. Обновите `models.py`: метод `create_custom_state()`
3. Обновите `entry.html`: добавьте опцию в select
4. Обновите `entry.js`: добавьте обработку в `updateCustomStatesList()`

#### Добавление нового графика
1. Создайте endpoint в `routes.py` для данных
2. Добавьте canvas в шаблон
3. Инициализируйте Chart.js в соответствующем JS-файле

## 📝 Примечания

- Проект разработан для защиты дипломной работы
- Все пароли хешируются через bcrypt
- Поддержка только прошлых дат (нельзя создавать записи на будущее)
- Форма записи идемпотентна — не сбрасывается после сохранения
- Все данные пользователя изолированы через `user_id`

## 🔒 Безопасность

- Пароли хранятся в виде хешей (bcrypt)
- Защита от SQL-инъекций через параметризованные запросы
- Flask-Login для управления сессиями
- Валидация всех пользовательских данных
- Защита маршрутов через декоратор `@login_required`

## 📄 Лицензия

Проект разработан для образовательных целей.

---

